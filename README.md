# 🤖 Discord Бот з Модерацією

Discord бот з повним набором модераційних команд для твого сервера!

## ⚡ Можливості

### 🛡️ Модерація
- `!warn @user причина` - Видати попередження користувачу
- `!warns @user` - Подивитися попередження користувача
- `!clearwarns @user` - Очистити всі попередження
- `!mute @user 10m причина` - Замутити користувача (підтримує m/h/d)
- `!unmute @user` - Розмутити користувача
- `!kick @user причина` - Кікнути користувача
- `!ban @user причина` - Забанити користувача
- `!unban userID` - Розбанити користувача за ID

### 🔧 Утиліти
- `!clear 10` - Видалити 10 повідомлень
- `!userinfo @user` - Інформація про користувача
- `!serverinfo` - Інформація про сервер
- `!ping` - Перевірити затримку бота

### 💬 Допомога
- `!help` - Показати всі команди

## 📋 Автоматичні дії
- **Автобан при 3 попередженнях** - Користувач автоматично банитися після 3-го warn

## 🚀 Як запустити на Railway

### Крок 1: Створи Discord бота
1. Перейди на https://discord.com/developers/applications
2. Натисни "New Application"
3. Дай ім'я боту і створи
4. Перейди в розділ "Bot"
5. Натисни "Add Bot"
6. Скопіюй токен (зберігай його в безпечному місці!)
7. Увімкни наступні Privileged Gateway Intents:
   - ✅ PRESENCE INTENT
   - ✅ SERVER MEMBERS INTENT
   - ✅ MESSAGE CONTENT INTENT

### Крок 2: Запроси бота на сервер
1. Перейди в розділ "OAuth2" → "URL Generator"
2. Вибери scope: `bot`
3. Вибери потрібні permissions:
   - Administrator (або окремо: Manage Messages, Kick Members, Ban Members, Moderate Members)
4. Скопіюй згенеровану URL і відкрий її в браузері
5. Вибери свій сервер і підтверди

### Крок 3: Розгорни на Railway
1. Створи аккаунт на https://railway.app
2. Натисни "New Project"
3. Вибери "Deploy from GitHub repo" (підключи свій GitHub)
4. Або вибери "Empty Project" і завантаж файли вручну
5. Додай змінну оточення:
   - Key: `DISCORD_TOKEN`
   - Value: `твій_токен_з_кроку_1`
6. Railway автоматично визначить Node.js і запустить бота!

### Альтернатива: Запуск локально

```bash
# Встанови Node.js (версія 16.9.0 або вища)

# Клонуй проект або завантаж файли

# Встанови залежності
npm install

# Створи файл .env і додай туди токен
echo "DISCORD_TOKEN=твій_токен" > .env

# Запусти бота
npm start
```

## 📁 Структура файлів

```
discord-bot/
├── index.js          # Головний файл бота
├── package.json      # Залежності проекту
├── data.json         # Автоматично створюється для збереження даних
├── .env.example      # Приклад файлу з токеном
└── README.md         # Ця інструкція
```

## ⚙️ Налаштування

### Зміна префіксу команд
У файлі `index.js` знайди рядок:
```javascript
const PREFIX = '!';
```
Зміни `!` на будь-який інший символ.

### Зміна кількості попереджень для автобану
У файлі `index.js` знайди рядок:
```javascript
if (warnCount >= 3) {
```
Зміни `3` на бажану кількість.

## 🔒 Безпека

- ❌ **НІКОЛИ** не діліся своїм токеном бота!
- ❌ Не викладай `.env` файл на GitHub
- ✅ Використовуй змінні оточення для токену
- ✅ Регулярно перевіряй доступ до бота

## 🐛 Вирішення проблем

### Бот не відповідає на команди
- Перевір, чи правильно введено токен
- Переконайся, що включені всі Privileged Gateway Intents
- Перевір, чи бот має права на сервері

### Бот не може кікати/банити
- Перевір права бота (має бути Administrator або відповідні permissions)
- Переконайся, що роль бота вище за ролі користувачів, яких він модерує

### Railway показує помилку
- Переконайся, що змінна `DISCORD_TOKEN` додана в налаштуваннях
- Перевір логи в Railway Dashboard

## 📞 Підтримка

Якщо виникли питання:
1. Перевір цей README
2. Перевір логи в Railway
3. Перевір Discord Developer Portal

## 📝 Ліцензія

ISC - вільне використання для твого сервера!

---

**Приємного використання! 🎉**
